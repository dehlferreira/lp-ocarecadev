<section
  id="cookie-consent"
  class="cookie-consent"
  role="dialog"
  aria-labelledby="cookie-consent-title"
  aria-describedby="cookie-consent-description"
  aria-live="polite"
>
  <div class="cookie-consent__content">
    <h2 id="cookie-consent-title">Sua privacidade</h2>
    <p id="cookie-consent-description">
      Podemos usar cookies de medição para entender como o site é utilizado e melhorar sua experiência. Você pode aceitar ou recusar agora e alterar esta escolha depois.
    </p>
  </div>
  <div class="cookie-consent__actions">
    <button id="cookie-consent-reject" class="cookie-consent__button" type="button">Recusar</button>
    <button id="cookie-consent-accept" class="cookie-consent__button" type="button">Aceitar</button>
  </div>
</section>

<style>
  .cookie-consent {
    position: fixed;
    z-index: 100;
    right: 1rem;
    bottom: 1rem;
    left: 1rem;
    display: flex;
    max-width: 42rem;
    margin-inline: auto;
    gap: 1rem;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem;
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 1rem;
    background: rgba(15, 18, 33, 0.97);
    box-shadow: 0 1rem 3rem rgba(0, 0, 0, 0.35);
  }

  .cookie-consent[hidden] {
    display: none;
  }

  .cookie-consent h2,
  .cookie-consent p {
    margin: 0;
  }

  .cookie-consent h2 {
    color: var(--color-text-primary);
    font-size: 1rem;
  }

  .cookie-consent p {
    margin-top: 0.4rem;
    color: var(--color-text-secondary);
    font-size: 0.875rem;
    line-height: 1.45;
  }

  .cookie-consent__actions {
    display: flex;
    flex: 0 0 auto;
    gap: 0.5rem;
  }

  .cookie-consent__button {
    min-height: 2.5rem;
    padding: 0.5rem 0.875rem;
    border: 1px solid rgba(255, 255, 255, 0.35);
    border-radius: 0.5rem;
    background: transparent;
    color: var(--color-text-primary);
    cursor: pointer;
    font: inherit;
    font-weight: 600;
  }

  .cookie-consent__button:hover,
  .cookie-consent__button:focus-visible {
    border-color: var(--color-primary-neon);
    outline: none;
  }

  @media (max-width: 34rem) {
    .cookie-consent {
      flex-direction: column;
      align-items: stretch;
    }

    .cookie-consent__actions {
      justify-content: stretch;
    }

    .cookie-consent__button {
      flex: 1;
    }
  }
</style>
