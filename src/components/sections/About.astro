---
import GlassCard from '../ui/GlassCard.astro';
import Icon from '../ui/Icon.astro';
import { Image } from 'astro:assets';
import perfilImage from '../../assets/images/perfil-novo.webp';

const instagramUrl = import.meta.env.INSTAGRAM_URL || 'https://www.instagram.com/ocarecadev';
const tiktokUrl = import.meta.env.TIKTOK_URL || 'https://www.tiktok.com/@ocarecadev';

const paragraphs = [
  'Olá, eu sou o <strong>André Luiz Ferreira</strong>, criador da <span class="text-neon">OCARECADEV</span>. Minha missão é transformar "sites bonitinhos" em verdadeiras máquinas de conversão.',
  'Com experiência profunda em desenvolvimento web e estratégias de alta performance, ajudo negócios a escalarem seus resultados com páginas projetadas milimetricamente para vender.',
];
---

<section id="about" class="section-about scrolly-section section-fade-top section-fade-bottom">
  <!-- Desktop: grid 2 colunas, 5 passos -->
  <div class="about-desktop">
    <div class="scrolly scrolly--5" style="--scrolly-track: 380vh;">
      <div class="scrolly__stage">
        <div class="container">
          <div class="about-grid">
            <div class="about-image scrolly-step from-left s-1">
              <div class="image-wrapper">
                <Image src={perfilImage} alt="André - OCARECADEV" class="profile-img" />
                <div class="neon-glow"></div>
              </div>
            </div>

            <div class="about-content">
              <h2 class="section-title scrolly-step about-reveal s-2">Sobre o Especialista</h2>

              <p class="about-desc scrolly-step about-reveal s-3" set:html={paragraphs[0]} />

              <p class="about-desc scrolly-step about-reveal s-4" set:html={paragraphs[1]} />

              <div class="about-footer scrolly-step about-reveal from-scale s-5">
                <div class="stats-grid">
                  <GlassCard class="stat-card" padding="1rem">
                    <h3>+50</h3>
                    <span>Projetos Lançados</span>
                  </GlassCard>
                  <GlassCard class="stat-card" padding="1rem">
                    <h3>100%</h3>
                    <span>Foco em ROI</span>
                  </GlassCard>
                </div>
                <div class="about-social">
                  <a href={instagramUrl} class="social-link" target="_blank" rel="noopener noreferrer" aria-label="Seguir no Instagram">
                    <Icon name="instagram" size={20} stroke={2} />
                    <span>Instagram</span>
                  </a>
                  <a href={tiktokUrl} class="social-link" target="_blank" rel="noopener noreferrer" aria-label="Seguir no TikTok">
                    <Icon name="tiktok" size={20} stroke={2} />
                    <span>TikTok</span>
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile: uma sequência — foto, título, texto, stats e social juntos -->
  <div class="about-mobile">
    <div class="scrolly scrolly--5 about-mobile-unified" style="--scrolly-track-mobile: 260vh;">
      <div class="scrolly__stage">
        <div class="container">
          <div class="about-mobile-stack">
            <div class="about-image scrolly-step from-scale s-1">
              <div class="image-wrapper">
                <Image src={perfilImage} alt="André - OCARECADEV" class="profile-img" />
                <div class="neon-glow"></div>
              </div>
            </div>

            <h2 class="section-title scrolly-step about-reveal s-2">Sobre o Especialista</h2>

            <p class="about-desc scrolly-step about-reveal s-3" set:html={paragraphs[0]} />

            <p class="about-desc scrolly-step about-reveal s-4" set:html={paragraphs[1]} />

            <div class="about-footer scrolly-step about-reveal from-scale s-5">
              <div class="stats-grid">
                <GlassCard class="stat-card" padding="1rem">
                  <h3>+50</h3>
                  <span>Projetos Lançados</span>
                </GlassCard>
                <GlassCard class="stat-card" padding="1rem">
                  <h3>100%</h3>
                  <span>Foco em ROI</span>
                </GlassCard>
              </div>
              <div class="about-social">
                <a href={instagramUrl} class="social-link" target="_blank" rel="noopener noreferrer" aria-label="Seguir no Instagram">
                  <Icon name="instagram" size={20} stroke={2} />
                  <span>Instagram</span>
                </a>
                <a href={tiktokUrl} class="social-link" target="_blank" rel="noopener noreferrer" aria-label="Seguir no TikTok">
                  <Icon name="tiktok" size={20} stroke={2} />
                  <span>TikTok</span>
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .section-about {
    position: relative;
    overflow: clip;
    background-color: #0a0a0a;
  }

  .about-desktop {
    display: none;
  }

  .about-mobile {
    display: block;
  }

  @media (min-width: 768px) {
    .about-desktop {
      display: block;
    }

    .about-mobile {
      display: none;
    }
  }

  .section-about .scrolly__stage {
    justify-content: flex-start;
  }

  @media (min-width: 768px) {
    .about-desktop .scrolly__stage {
      justify-content: center;
      padding-top: 0;
      padding-bottom: 0;
    }
  }

  /* Passos colapsados até revelar — evita reservar altura fantasma */
  .about-reveal {
    overflow: hidden;
  }

  @media (prefers-reduced-motion: no-preference) {
    @supports ((animation-timeline: view()) and (animation-range: contain)) {
      .about-reveal {
        animation-name: about-reveal !important;
      }

      .about-reveal.from-scale {
        animation-name: about-reveal-scale !important;
      }
    }
  }

  @keyframes about-reveal {
    from {
      opacity: 0;
      transform: translateY(24px);
      max-height: 0;
    }

    to {
      opacity: 1;
      transform: none;
      max-height: 420px;
    }
  }

  @keyframes about-reveal-scale {
    from {
      opacity: 0;
      transform: scale(0.94);
      max-height: 0;
    }

    to {
      opacity: 1;
      transform: none;
      max-height: 420px;
    }
  }

  .about-grid {
    display: grid;
    gap: 2rem;
    align-items: center;
    width: 100%;
  }

  @media (min-width: 768px) {
    .about-grid {
      grid-template-columns: minmax(260px, 300px) 1fr;
      gap: 3.5rem;
      align-items: center;
    }

    .about-desktop .image-wrapper {
      width: 280px;
      height: 280px;
    }

    .about-desktop .section-title {
      font-size: 2.5rem;
      margin-bottom: 1.25rem;
    }

    .about-desktop .about-desc {
      font-size: 1.125rem;
      line-height: 1.8;
      margin-bottom: 1.25rem;
    }

    .about-desktop .about-footer {
      margin-top: 0.5rem;
      gap: 1.25rem;
    }

    .about-desktop .stats-grid {
      gap: 1.15rem;
    }

    .about-desktop :global(.stat-card) {
      padding: 1.25rem !important;
    }

    .about-desktop :global(.stat-card h3) {
      font-size: 2rem;
    }

    .about-desktop :global(.stat-card span) {
      font-size: 0.875rem;
    }

    .about-desktop .social-link {
      font-size: 0.925rem;
      padding: 0.65rem 1.15rem;
    }

    .about-desktop .social-link :global(.icon) {
      width: 22px;
      height: 22px;
    }
  }

  .about-image {
    display: flex;
    justify-content: center;
  }

  .image-wrapper {
    position: relative;
    width: 200px;
    height: 200px;
    border-radius: 50%;
  }


  .profile-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 25%;
    border-radius: 50%;
    position: relative;
    z-index: 2;
    border: 3px solid var(--glass-border);
  }

  .neon-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    background-color: var(--color-primary-neon);
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.3;
    z-index: 1;
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: 0;
    min-width: 0;
  }

  .section-title {
    font-size: 1.75rem;
    font-weight: 800;
    color: var(--color-text-primary);
    margin: 0 0 1rem;
    line-height: 1.2;
  }


  .about-desc {
    color: var(--color-text-secondary);
    font-size: 1rem;
    margin: 0 0 1rem;
    line-height: 1.7;
  }


  .about-footer {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 0.25rem;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.85rem;
  }


  :global(.stat-card) {
    text-align: center;
  }

  :global(.stat-card h3) {
    font-size: 1.5rem;
    color: var(--color-primary-neon);
    margin: 0 0 0.25rem;
  }


  :global(.stat-card span) {
    color: var(--color-text-secondary);
    font-weight: 600;
    font-size: 0.8rem;
  }

  .about-social {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
  }

  .social-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.55rem 0.95rem;
    border-radius: 100px;
    border: 1px solid var(--glass-border);
    background: var(--glass-bg);
    color: var(--color-text-primary);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.85rem;
    transition: border-color 0.25s ease, color 0.25s ease, background 0.25s ease, box-shadow 0.25s ease;
  }

  .social-link:hover {
    border-color: var(--color-primary-neon);
    color: var(--color-primary-neon);
    background: rgba(0, 255, 157, 0.06);
    box-shadow: 0 0 20px rgba(0, 255, 157, 0.12);
  }

  .social-link :global(.icon) {
    color: var(--color-primary-neon);
  }

  /* Mobile: tudo num bloco só, centralizado */
  @media (max-width: 767px) {
    .about-mobile-unified .scrolly__stage {
      justify-content: center;
      padding-top: 0;
      padding-bottom: 0;
    }

    .about-mobile-stack {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 1.15rem;
      width: 100%;
    }

    .about-mobile-stack .image-wrapper {
      width: 160px;
      height: 160px;
    }

    .about-mobile-stack .section-title {
      font-size: 1.5rem;
      margin: 0;
    }

    .about-mobile-stack .about-desc {
      font-size: 0.9rem;
      line-height: 1.55;
      margin: 0;
      text-align: left;
      max-width: 100%;
    }

    .about-mobile-stack .about-footer {
      margin-top: 0.35rem;
      gap: 1rem;
      width: 100%;
    }

    .about-mobile-stack :global(.stat-card h3) {
      font-size: 1.35rem;
    }

    .about-mobile-stack :global(.stat-card) {
      padding: 0.75rem !important;
    }

    .about-mobile-stack .about-social {
      justify-content: center;
    }

    .about-mobile-unified .about-image.scrolly-step {
      animation-name: about-photo-enter !important;
    }

    .about-mobile-unified .about-reveal {
      animation-name: about-reveal-mobile !important;
    }

    .about-mobile-unified .about-footer.about-reveal {
      animation-name: about-reveal-footer-mobile !important;
    }

    @keyframes about-photo-enter {
      from {
        opacity: 0;
        transform: scale(0.92);
        max-height: 0;
        overflow: hidden;
      }

      to {
        opacity: 1;
        transform: none;
        max-height: 200px;
        overflow: visible;
      }
    }

    @keyframes about-reveal-mobile {
      from {
        opacity: 0;
        transform: translateY(20px);
        max-height: 0;
        overflow: hidden;
      }

      to {
        opacity: 1;
        transform: none;
        max-height: 180px;
        overflow: visible;
      }
    }

    @keyframes about-reveal-footer-mobile {
      from {
        opacity: 0;
        transform: scale(0.94);
        max-height: 0;
        overflow: hidden;
      }

      to {
        opacity: 1;
        transform: none;
        max-height: 260px;
        overflow: visible;
      }
    }
  }
</style>
